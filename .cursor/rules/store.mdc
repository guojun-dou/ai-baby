---
description: pinia store rules
globs:
  - 'stores/**/*.ts'
  - 'api/*.ts'
alwaysApply: false
---

你正在开发 Pinia 状态管理和 composition api 封装。

技术要求：

- Pinia
- TypeScript
- composition api
- uni-app兼容

## API规范

页面禁止直接调用云函数。

统一：

- api/
  - goods.ts
  - order.ts
  - admin.ts

页面仅调用 api 层。

---

## Store规范

- 使用 defineStore
- 使用 setup 风格
- 使用 ref/computed
- 使用 uni.setStorageSync 持久化
- 使用 uni.getStorageSync 读取缓存
- store 仅保存状态
- 数据请求放 api
- 禁止巨型 store

禁止：

- 不要使用 Vuex
- 不要使用 localStorage
- 不要使用 Redux
- 不要使用 class 风格

购物车 Store：

必须包含：

- cartList
- addCart
- removeCart
- clearCart
- changeCount
- totalPrice
- totalCount

用户 Store：

必须包含：

- userInfo
- token
- login
- logout

代码要求：

- 输出完整 store
- 包含 TypeScript 类型
- 包含持久化逻辑
- 代码可直接运行

数据结构要求：

购物车商品：

{
\_id,
title,
price,
cover,
count
}

保持：

- Store 简洁
- 方法清晰
- 避免过度抽象
